---
import parseMarkdown from '../utils/parse-markdown'
const { content, tag: Tag = 'div' } = Astro.props
const noParagraph = Astro.props.noParagraph === true
let html = await parseMarkdown(content, noParagraph)
---
<Tag set:html={html}></Tag>
